<UserControl
    x:Class="FancyCards.Views.TextReplacementRuleDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FancyCards.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FancyCards.ViewModels"
    Width="200"
    Height="auto"
    d:DataContext="{d:DesignInstance vm:TextReplacementRuleDetailViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid
            Margin="0,5"
            VerticalAlignment="Top"
            Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="15" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding Original, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <materialDesign:PackIcon
                Grid.Column="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Kind="ArrowRightBold" />
            <TextBox Grid.Column="2" Text="{Binding Replacement, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        </Grid>

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="5,0"
                Command="{Binding SaveCommand}"
                Content="{Binding Header}" />
            <Button Command="{Binding CloseModalCommand}" Content="Cancel" />
        </StackPanel>
    </Grid>

</UserControl>
