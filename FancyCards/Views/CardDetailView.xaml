<UserControl
    x:Class="FancyCards.Views.CardDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:FancyCards.Behaviors"
    xmlns:controls="clr-namespace:FancyCards.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactions="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:FancyCards.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FancyCards.ViewModels"
    d:Background="White"
    d:DataContext="{d:DesignInstance vm:CardDetailViewModel}"
    mc:Ignorable="d">
    <Grid Width="400" Height="auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <controls:HintControl
                    x:Name="Prefix"
                    Grid.Column="0"
                    HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                    HintText="Prefix"
                    SuffixText="{Binding PrefixText.Length}">
                    <TextBox
                        materialDesign:HintAssist.Hint=""
                        materialDesign:HintAssist.HintPaddingBrush="Transparent"
                        AcceptsReturn="True"
                        Text="{Binding PrefixText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        TextWrapping="Wrap" />
                </controls:HintControl>

                <controls:HintControl
                    Grid.Column="1"
                    HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                    HintText="Suffix"
                    SuffixText="{Binding SuffixText.Length}">
                    <TextBox
                        materialDesign:HintAssist.Hint=""
                        materialDesign:HintAssist.HintPaddingBrush="Transparent"
                        AcceptsReturn="True"
                        Text="{Binding SuffixText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        TextWrapping="Wrap" />
                </controls:HintControl>
            </Grid>

            <controls:HintControl
                Margin="0,0,0,5"
                HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                HintText="Front"
                SuffixText="{Binding FrontText.Length}">
                <TextBox
                    materialDesign:HintAssist.Hint=""
                    materialDesign:HintAssist.HintPaddingBrush="Transparent"
                    AcceptsReturn="True"
                    Text="{Binding FrontText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    TextAlignment="Left"
                    TextWrapping="Wrap" />
            </controls:HintControl>

            <controls:HintControl
                Margin="0,0,0,5"
                HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                HintText="Back"
                SuffixText="{Binding BackText.Length}">
                <TextBox
                    materialDesign:HintAssist.Hint=""
                    materialDesign:HintAssist.HintPaddingBrush="Transparent"
                    AcceptsReturn="True"
                    Text="{Binding BackText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    TextAlignment="Left"
                    TextWrapping="Wrap" />
            </controls:HintControl>

            <controls:HintControl
                Margin="0,0,0,5"
                HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                HintText="Comment"
                SuffixText="{Binding CommentText.Length}">
                <TextBox
                    materialDesign:HintAssist.Hint=""
                    materialDesign:HintAssist.HintPaddingBrush="Transparent"
                    AcceptsReturn="True"
                    Text="{Binding CommentText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    TextAlignment="Left"
                    TextWrapping="Wrap" />
            </controls:HintControl>

            <controls:HintControl
                Margin="0,0,0,5"
                HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                HintText="Message"
                SuffixText="{Binding MessageText.Length}">
                <TextBox
                    materialDesign:HintAssist.Hint=""
                    materialDesign:HintAssist.HintPaddingBrush="Transparent"
                    AcceptsReturn="True"
                    Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    TextAlignment="Left"
                    TextWrapping="Wrap" />
            </controls:HintControl>


            <Grid Margin="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Column="0"
                    Margin="0,0,1,0"
                    Orientation="Vertical">
                    <controls:HintControl HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}" HintText="State">
                        <ComboBox
                            Padding="10"
                            materialDesign:HintAssist.Hint=""
                            materialDesign:HintAssist.HintPaddingBrush="Transparent"
                            ItemsSource="{Binding States}"
                            SelectedValue="{Binding SelectedState, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    </controls:HintControl>
                </StackPanel>

                <StackPanel
                    Grid.Column="1"
                    Margin="1,0,0,0"
                    Orientation="Vertical">
                    <controls:HintControl HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}" HintText="Review date">
                        <DatePicker
                            Padding="10"
                            materialDesign:HintAssist.Hint=""
                            materialDesign:HintAssist.HintPaddingBrush="Transparent"
                            SelectedDate="{Binding NextReviewDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}" />
                    </controls:HintControl>

                    <controls:HintControl
                        Margin="0,2,0,0"
                        HintBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                        HintText="Date created">
                        <DatePicker
                            Padding="10"
                            materialDesign:HintAssist.Hint=""
                            materialDesign:HintAssist.HintPaddingBrush="Transparent"
                            IsHitTestVisible="False"
                            SelectedDate="{Binding DateCreated}"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}" />
                        <interactions:Interaction.Triggers>
                            <interactions:DataTrigger Binding="{Binding CardAction}" Value="Create">
                                <interactions:ChangePropertyAction PropertyName="Visibility" Value="Collapsed" />
                            </interactions:DataTrigger>
                            <interactions:DataTrigger Binding="{Binding CardAction}" Value="Update">
                                <interactions:ChangePropertyAction PropertyName="Visibility" Value="Visible" />
                            </interactions:DataTrigger>
                        </interactions:Interaction.Triggers>
                    </controls:HintControl>

                </StackPanel>
            </Grid>

            <local:AudioSamplerView DataContext="{Binding AudioSamplerViewModel}" />



        </StackPanel>
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="5,0"
                Command="{Binding SaveCardCommand}"
                Content="{materialDesign:PackIcon Kind=ContentSave}" />
            <Button Command="{Binding CancelCommand}" Content="{materialDesign:PackIcon Kind=Close}" />
        </StackPanel>
    </Grid>

</UserControl>
