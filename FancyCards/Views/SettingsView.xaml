<UserControl
    x:Class="FancyCards.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FancyCards.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FancyCards.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:FancyCards.ViewModels"
    d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel
        Width="200"
        Height="200"
        Orientation="Vertical">
        <controls:CustomNumericControl
            Margin="0,0,0,5"
            DefaultValue="10"
            Frequency="1"
            MaxValue="100"
            MinValue="0"
            Value="{Binding TrainingLearnCards, Mode=TwoWay}">
            <controls:CustomNumericControl.ContentTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" CornerRadius="3">
                        <TextBlock>
                            <Run Text="Training learn cards:" />
                            <Run Text="{Binding Path=.}" />
                        </TextBlock>
                    </Border>
                </DataTemplate>
            </controls:CustomNumericControl.ContentTemplate>
        </controls:CustomNumericControl>

        <controls:CustomNumericControl
            Margin="0,0,0,5"
            DefaultValue="10"
            Frequency="1"
            MaxValue="100"
            MinValue="0"
            Value="{Binding TrainingReviewCards, Mode=TwoWay}">
            <controls:CustomNumericControl.ContentTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" CornerRadius="3">
                        <TextBlock>
                            <Run Text="Training review cards:" />
                            <Run Text="{Binding Path=.}" />
                        </TextBlock>
                    </Border>
                </DataTemplate>
            </controls:CustomNumericControl.ContentTemplate>
        </controls:CustomNumericControl>

        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button
                Grid.Row="2"
                Width="60"
                Height="30"
                Margin="5,0,0,0"
                Padding="0"
                Command="{Binding SaveCommand}"
                Content="Save" />
            <Button
                Grid.Row="2"
                Width="60"
                Height="30"
                Margin="5,0,0,0"
                Padding="0"
                Command="{Binding CancelCommand}"
                Content="Cancel" />
        </StackPanel>


    </StackPanel>
</UserControl>
